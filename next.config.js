/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["everai-collection-v0.s3.us-west-2.amazonaws.com", "i.seadn.io"],
    },
};

module.exports = nextConfig;
